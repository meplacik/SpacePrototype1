# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SpacePrototype1' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SpacePrototype1
  pod 'PieCharts'
  target 'SpacePrototype1Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SpacePrototype1UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
